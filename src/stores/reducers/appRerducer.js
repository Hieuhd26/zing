import actionType from "../actions/actionType";

const initState = {
    banner:[]
}

export const appReducer = (state = initState, action)=>{
        switch (action.type) {
            case action:
                
                break;
        
            default:
                return state;
        }
}